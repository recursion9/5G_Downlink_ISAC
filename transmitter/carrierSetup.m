function [carrier] = carrierSetup()
% CARRIER SETUP:
% Create a carrier configuration object. This object controls the numerology, 
% such as, the subcarrier spacing, bandwidth, and cyclic prefix (CP) length. 
carrier = nrCarrierConfig;
end

